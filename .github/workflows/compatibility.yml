name: Compatibility

on:
  push:
    branches: [alpha, master]
  pull_request:
  workflow_dispatch:

jobs:
  amber-cli:
    name: amber_cli compatibility (${{ matrix.os }})
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, macos-15]

    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803  # v6

      - name: Install Crystal
        uses: oprypin/install-crystal@d8ef131ecec0352ce0e39b81b0a6d95def58fe2f  # v1
        with:
          crystal: latest

      - name: Validate amber_cli compatibility
        env:
          AMBER_CLI_REF: main
        run: ./scripts/validate_amber_cli_compatibility.sh
