# class Shards::LockfileDiffer

## Class Methods

### `diff(from_packages : Array(Package), to_packages : Array(Package)) : Array(Change)`

Compare two package sets and produce changes

## Types

- `Shards::LockfileDiffer::Change` (struct)
- `Shards::LockfileDiffer::Status` (enum)

